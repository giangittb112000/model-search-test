services:
  nlp:
    build: .
    image: search-model-nlp:latest
    working_dir: /app
    command: ["sleep", "infinity"]
    env_file:
      - path: .env
        required: true
    environment:
      - PYTHONPATH=/app

# FILE MẪU → make env-init-gpu tạo .env
# Cần: nvidia-container-toolkit + PyTorch+CUDA trong image (train GPU thật)
# Xem docs/env.md

NER_TRAIN_MODE=gpu
NER_RUN_MODE=cpu
NER_GPU_ID=0
NER_GPU_ALLOCATOR=pytorch
